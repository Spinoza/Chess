#include "chessboard.hh"

std::bitset<64> Chessboard::moveBishopNE(std::bitset<64> test, int dir) const
{
    return test;
}

std::bitset<64> Chessboard::moveBishopNW(std::bitset<64> test, int dir) const
{
    return test;
}

std::bitset<64> Chessboard::moveBishopSE(std::bitset<64> test, int dir) const
{
    return test;
}

std::bitset<64> Chessboard::moveBishopSW(std::bitset<64> test, int dir) const
{
    return test;
}
